const fs = require('fs');

// We will test SVG generation
function generateTestSVG(url) {
    const QRCode = require('./lib/qrcode.min.js');
    console.log('Testing QRCode module...');
}
generateTestSVG();
